# Dynamic Store Map Checklist

- [x] Task A — `lua/game/game_state.lua` — Change `INIT_COLS` from `5` to `7` and `INIT_ROWS` from `2` to `5`.

- [x] Task B — `lua/game/store.lua` — Four changes: (1) Change `GRID_ORIGIN_Y` from `4.5` to `2.5`. (2) Change `GRID_SPACING_Y` from `-1.0` to `+1.0`. (3) Remove `MAX_ROWS` and its guard in `grow()`. (4) Rewrite `grow()` to add one full row of `self._cols` slots at a time instead of one slot — new row number is `active_rows() + 1`, loop cols 1–`self._cols`, compute `px = GRID_ORIGIN_X + (col-1)*GRID_SPACING_X` and `py = GRID_ORIGIN_Y + (next_row-1)*GRID_SPACING_Y`. (5) Add `Store:active_rows()` returning `math.ceil(#self._slots / self._cols)`.

- [x] Task C — `lua/game/scenes/store_scene.lua` — Rewrite the map and positional constants to match the 7-wide, southward-growing store. All five changes must land together: (1) Replace the hardcoded `MAP_GRID` constant with a `build_map_grid(n)` function. Map is 14 cols wide (1 left wall + 7 store cols at Lua 2–8 + 1 separator at Lua 9 + 4 cashier cols at Lua 10–13 + 1 right wall at Lua 14). Map has `n + 2` rows: row 1 = top wall; rows 2 to n+1 = slot rows; row n+2 = south wall. Store cols (Lua 2–8) and cashier cols (Lua 10–13) are always `0` in slot rows. Separator col (Lua 9) is `0` (open/passage) only in the two southernmost slot rows (slot rows `n` and `n-1`); `1` everywhere else. (2) Update `CASHIER_THRESH` from `7.0` to `9.0`. (3) Update `CASHIER_POS_X` from `9.5` to `11.5`. (4) Update `PLAYER_START_X` from `3.5` to `5.0`. (5) Add a helper `local function store_geometry(n)` that returns `front_y = 2.5 + (n-1)`, `player_y = front_y` (player starts in the southernmost slot row, which is also a passage row), `cashier_y = front_y`. Replace the hardcoded `PLAYER_START_Y` and `CASHIER_POS_Y` with values from this function using the initial row count. In `on_enter()`, call `store:active_rows()`; if it differs from `self._last_active_rows`, rebuild `self.map = Map.new(build_map_grid(n))`, update `self.player3d.y`, update `self._cashier_y`, and update `self._last_active_rows`. Use `self._cashier_y` wherever `CASHIER_POS_Y` was referenced in draw/sprite code.

- [x] Task D — `lua/game/scenes/buy_scene.lua` — Update the Expand Slot entry in `CATALOGUE`: change `description` from `"Adds one new slot to the\nright end of the store."` to `"Adds a new row of 7 slots\nto the front of the store."`.
