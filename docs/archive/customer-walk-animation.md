## Customer Walk Animation Checklist

- [x] Task A — `lua/game/scenes/store_scene.lua` — Add three new constants near the top (`CASHIER_ENTRY_X = 1.5`, `CUST_WALK_SPEED = 2.5`, `CUST_WALK_FRAME_T = 0.15`). In `_setup_store`, change `Customer.new(0, -1, 0)` to `Customer.new(100, -1000, 0)` and initialize four new fields on self: `_cust_3d_x = CASHIER_POS_X`, `_cust_anim = nil`, `_cust_walk_timer = 0`, `_cust_walk_frame = false`.

- [x] Task B — `lua/game/scenes/store_scene.lua` — In `update`, replace the snap hack (the two if-blocks after `customer:update(dt)` that force walking_in→waiting and walking_out→idle) with full walk animation logic: (1) detect `state == "walking_out"` BEFORE calling `customer:update(dt)` and set `_cust_anim = "out"` if not already set; (2) after `customer:update(dt)`, advance `_cust_3d_x` and toggle `_cust_walk_frame` on timer when `_cust_anim ~= nil`; (3) on walk-in completion set `customer.state = "waiting"` and `bubble.visible = true`, nil out `_cust_anim`; (4) on walk-out completion set `customer.state = "idle"`, `sprite.visible = false`, `bubble.visible = false`, `heart_bubble.visible = false`, nil out `_cust_anim`. Also change the spawn timer block so show() is followed immediately by `self._cust_3d_x = CASHIER_ENTRY_X; self._cust_anim = "in"`, and guard the whole spawn timer block with `not self._customer:active() and not self._cust_anim`.

- [x] Task C — `lua/game/scenes/store_scene.lua` — In `draw`, update the customer billboard: (1) change the visibility gate from `customer:active()` to `customer:active() or self._cust_anim ~= nil`; (2) replace the hardcoded `x = CASHIER_POS_X` with `x = self._cust_3d_x`; (3) replace the hardcoded `image = A.customer` with `image = (self._cust_anim and self._cust_walk_frame) and A.customer_walk or A.customer`.

- [x] Task D — `lua/game/scenes/store_scene.lua` — Add `not self._cust_anim` guards to the three cashier-zone call sites: in `_handle_pick_up_down` guard the `customer:arrived()` branch; in `_handle_interact` guard the `customer:arrived()` branch; in `_hud_labels` change `in_cash` to also require `not self._cust_anim` so labels and key hints disappear while the customer is walking.
